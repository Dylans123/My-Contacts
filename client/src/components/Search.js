import React, { Component } from 'react';
import api from '../api'

export default class Create extends Component {

    constructor(props) {
        super(props);
        this.state = {
            name: '',
        }
    }

    render() {
        return (

        )
    }
}